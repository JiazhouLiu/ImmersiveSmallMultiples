using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LightWithShelf : MonoBehaviour {


	// Update is called once per frame
	void Update () {

        //Transform shelf = GameObject.Find("Shelf").transform;
        //if (shelf != null) {
        //    //this.transform.position = shelf.position - (shelf.forward * 2) + (shelf.up * 2f);
        //    this.transform.position = Camera.main.transform.position;


        //    Vector3 shelfPos = shelf.position + Vector3.up * 1.5f;
        //    //Vector3 finalPos = new Vector3(shelfPos.x, this.transform.position.y, shelfPos.z);
        //    Vector3 offset = this.transform.position - shelfPos;
        //    this.transform.LookAt(shelfPos);
        //}
        
    }
}
